services:
  - type: web
    name: medassist-ai-backend
    env: python

    buildCommand: |
      chmod +x build.sh
      ./build.sh

    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT